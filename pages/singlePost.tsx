import * as React from 'react';
import Layout from '@containers/Layout';

const SinglePost = () => {
    return (
        <Layout>
            <div>contato</div>
        </Layout>
    );
};

export default SinglePost;
